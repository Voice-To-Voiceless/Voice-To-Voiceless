import { StyleSheet } from 'react-native';

export const footerStyles = StyleSheet.create({
  footerText: {
    color: '#889289',
    fontSize: 11,
    lineHeight: 16,
    marginTop: 'auto',
    paddingBottom: 12,
    textAlign: 'center',
  },
});